using System.Collections; 
using System.Collections.Generic; 
using UnityEngine; 
 
public class Checkpoints : MonoBehaviour 
{ 
    public int cpNumber; // variable to keep track of the checkpoint number for each of the checkpoints 
    // it is in a seperate class so that it can be attached to ecah of the checkpoints and assigned a different value 
} 
